# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Development
"on":
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: npm ci && npm run build
        env:
          VUE_APP_API_HOST: "https://prod-fake-api-host.com"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CAFENOMAD_153014 }}"
          channelId: live
          projectId: cafenomad-153014
          target: development
